import { StyleSheet } from "react-native"
import { getNewDimensions } from "../../utils/dimansions/dimansions"


export const searchStyle = StyleSheet.create({
    containerSearchbox: {
         width: '80%',  margin: 'auto',
         padding: 10, 
         left: '5%', color: '#555', top: 100 
    }
})